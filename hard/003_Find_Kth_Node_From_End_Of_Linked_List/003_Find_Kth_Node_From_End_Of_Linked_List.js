// Find the kth node from the end in a singly linked list.
// You can only pass through the list one time.
// Assume k >= 1
// Example:
// input: 1->2->3->4->5, k = 2
// output: 4 is the 2nd node from the end of the list

// For linked list implementation see the __data_structures__ folder

const findKthNode = (linkedList, k) => {};

module.exports = { findKthNode };
