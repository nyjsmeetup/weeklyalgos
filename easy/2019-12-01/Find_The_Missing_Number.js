/* Given an array of length 'n' containing distinct numbers that range from 
   0 to 'n' (n + 1 possible values),
   find the missing number in the array */

// example:
// input = [1, 3, 2];
// output = 0;

// input = [0,5,6,3,1,2]
// output = 4

const findMissingNum = arr => {
  return -1;
};

module.exports = { findMissingNum };
