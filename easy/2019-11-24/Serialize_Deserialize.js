// https://en.wikipedia.org/wiki/Serialization

// Given an array of strings output a serialized string that can be properly deserialized back to the same array

// Example:
// const someSerializedString = serialize(['an', 'array', 'of', 'strings'])
// deserialize(someSerializedString) should return ['an','array', 'of', 'strings']

const serialize = arrOfStrings => {};

const deserialize = serializedString => {};

module.exports = { serialize, deserialize };
